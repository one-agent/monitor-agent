# Spring Boot Configuration
server.port=8080
spring.application.name=monitor-agent

# Logging
logging.level.root=INFO
logging.level.com.oneagent.monitor=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# LLM Configuration (Custom OpenAI Compatible Endpoint)
monitor.llm.api-key=${LLM_API_KEY:your-api-key-here}
monitor.llm.base-url=${LLM_BASE_URL:http://localhost:11434/v1}
monitor.llm.model-name=${LLM_MODEL_NAME:gpt-3.5-turbo}
monitor.llm.temperature=0.7
monitor.llm.max-tokens=2000
monitor.llm.stream=false

# Feishu Webhook Configuration
monitor.feishu.webhook-url=${FEISHU_WEBHOOK_URL:https://open.feishu.cn/open-apis/bot/v2/hook/placeholder}

# Apifox API Configuration
monitor.apifox.api-url=${APIFOX_API_URL:https://api.apifox.com}
monitor.apifox.api-token=${APIFOX_API_TOKEN:your-apifox-token-here}
monitor.apifox.project-id=${APIFOX_PROJECT_ID:your-project-id-here}
monitor.apifox.folder-id=${APIFOX_FOLDER_ID:your-folder-id-here}

# Knowledge Base Configuration
monitor.knowledge.path=${KNOWLEDGE_PATH:src/main/resources/knowledge}

# Input/Output Paths
monitor.path.input-path=inputs/inputs.json
monitor.path.output-path=outputs/results.json

# Embedding Model Configuration (for RAG)
#monitor.embedding.api-key=${EMBEDDING_API_KEY:your-embedding-key}
#monitor.embedding.model-name=${EMBEDDING_MODEL_NAME:text-embedding-v2}

# Studio Configuration
# Enable Studio support
monitor.studio.enabled=true

# Studio server URL
monitor.studio.studio-url=http://localhost:3000

# Project name (for project identifier in Studio)
monitor.studio.project-name=MonitorAgent

# Run name (to distinguish different agents)
monitor.studio.run-name=demo_
